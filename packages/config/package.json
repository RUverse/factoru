{
  "name": "@factoru/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript configuration for every Factoru workspace package.",
  "type": "module",
  "files": [
    "tsconfig.base.json",
    "tsconfig.lib.json"
  ],
  "scripts": {
    "build": "echo \"@factoru/config has no build output\"",
    "typecheck": "echo \"@factoru/config has no TypeScript sources\"",
    "test": "echo \"@factoru/config has no tests\""
  }
}
