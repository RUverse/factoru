/**
 * The Factoru Desktop application version, reported in the handshake.
 * `version.test.ts` fails if it drifts from `package.json`.
 */
export const DESKTOP_VERSION = '0.0.0'

export const DESKTOP_NAME = 'factoru-desktop'
