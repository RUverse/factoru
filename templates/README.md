# `templates/` (not yet implemented)

Factoru Factory Template manifests that compose a pinned Gas City pack with
Worker Types, named model slots, tool/memory policies, Formula defaults, capsule
requirements, and UI metadata.

`templates/software-project` is created during **Milestone 3 — Product shell and
persistent Project Manager**. Manifests are trusted configuration and must be
pinned and reviewed; see [AGENTS.md](../AGENTS.md).
